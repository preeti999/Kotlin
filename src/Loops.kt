fun main() {
    //#1
    var count = 5
    while (count >= 1) {
//        println("Hi")
        count--
    }

    //#2
    val number = 2
    var index = 1
    while (index <= 10) {
        //println("${number}" + " * " + "${index}" + " = " + number * index)
        index++
    }

    //#3 DO WHILE LOOP
    var variable=6
    do{
        println("DO THE PROCESS")
        variable++
    }while (variable<5)
}